galaxy_info:
  author: Lukas Pustina
  namespace: rheinwerk
  role_name: bosun_all_in_one
  description: Installs Bosun with OpenTSDB as Docker Containers
  company: Rheinwerk
  license: license (GPLv3)
  min_ansible_version: "2.7"
  platforms:
    - name: Ubuntu
      versions:
        - trusty
  galaxy_tags:
    - bosun
    - opentsdb
    - system
dependencies:
  - { role: Rheinwerk.docker_ubuntu, docker_pkg_name: "{{ _docker_install.docker_pkg_name }}", update_docker_package: "{{ _docker_install.update_docker_package }}", docker_opts: "{{ _docker_install.docker_opts }}" }
